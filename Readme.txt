if you are not running mysql server with proper data formatting then
1. change the MYSQL ip and password as required
 other wise click on route.py and run


required pip install

flask, pandas, mysql-connector-python

The data that was in the server is formed in a CSV file.
where we crawled the CVE information and attatched it with the dataframe using pandasg